The 'search' script aims to search in multiple JSON files, something you can
configure as address book source. Let's say e.g. one source is Google
Contacts, a second source is from LDAP.

This is intentionally written in Python3 (not 2).
